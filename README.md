# colmap-gps-semantic-octomap
Using colmap plus gps(rtk) to reconstruct sparse map, then fuse the dense depth and semantic information of each frame to get a semantic occupancy map represented by octomap
